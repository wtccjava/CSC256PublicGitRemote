language: python
  python:
   - "3.9"
   - "nightly"

  install:
   - "pip install pytest"
   - "pip install -r requirements.txt"
 
  script: python -m pytest gitremote

  deploy:
    provider: pages
    skip-cleanup: true
    github-token: $GITHUB_TOKEN
    on:
      branch: gh-pages
